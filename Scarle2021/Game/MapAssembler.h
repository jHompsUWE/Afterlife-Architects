#pragma once
#include <vector>
#include <random>

#include "PerlinNoise.h"

class MapAssembler
{
public:
    
    //static std::vector<std::vector<float>> GenerateNoiseMap(const int& map_width, const int& map_height, Random)
};